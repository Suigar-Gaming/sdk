---
'@suigar/mcp': patch
---

Allow MCP read-only plans and transaction summaries to work with SDK configs that use MVR-backed package defaults instead of concrete package ids. Transaction summaries also preserve configured gas budget inputs as raw MIST plus SUI display values.
