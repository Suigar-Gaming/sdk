---
'@suigar/sdk': minor
---

Use generated MVR package defaults for Suigar game, referral, and core bindings while keeping optional package id overrides for deployments that need them. `nftV1` remains the only statically configured package id. Game parameter settings lookup and PvP Coinflip registry lookup now also respect overridden game package ids.
