---
'@suigar/sdk': minor
---

Use generated MVR package defaults for Suigar game, referral, and core bindings while keeping optional package id overrides for deployments that need them. `nftV1` remains the only statically configured package id.
